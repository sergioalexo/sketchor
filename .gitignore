# dependencies / build output
node_modules/
dist/

# rust build output (tauri shell + native thumbnailer)
target/

# macOS Quick Look extension build output (.appex, sample binary)
native/dxf-quicklook/build/

# logs / os cruft
*.log
.DS_Store
Thumbs.db

# Plugin signing private keys (never commit)
plugin-key.json
*.sketchor-key.json

# occt-import-js rebuild workspace (upstream checkout, emsdk, object files — see native/occt-import-js-build/README.md)
native/occt-import-js-build/src/
native/occt-import-js-build/emsdk/
native/occt-import-js-build/build/
