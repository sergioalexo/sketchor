# dependencies / build output
node_modules/
dist/

# rust build output (tauri shell + native thumbnailer)
target/

# tauri updater signing keys — NEVER commit the private key
.tauri-keys/

# logs / os cruft
*.log
.DS_Store
Thumbs.db
