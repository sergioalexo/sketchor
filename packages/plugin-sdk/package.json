{
  "name": "@sketchor/plugin-sdk",
  "version": "0.1.0",
  "description": "Typed authoring SDK for Sketchor plugins. Versioned in lockstep with the host API.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@sketchor/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
